<h1><%= t(:create_domain) %></h1>
<div>
    <%= simple_form_for( @domain,) do |f| %>
        <div class="form-group">  
            <%= f.input     :name, :html => { :class => "form-horizontal" } %>
            <%= f.text_area :description, :class => "form-control" %>
            <%= f.button    :submit, :class => "btn btn-default" %>
        </div>
  <% end %>
</div>

<div id="result">
</div>
